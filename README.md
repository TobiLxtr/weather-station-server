# weather-station-server
Docker setup for a Raspberry Pi using InfluxDB and Grafana to act as a server for a home IoT weather station.

## Setup
Add a `.env` in the same structre as the `.env.example` file to your project folder containing the initial credentials.
